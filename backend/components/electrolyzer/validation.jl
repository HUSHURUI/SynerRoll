function validate_special_rules(::Type{Electrolyzer}, comp_dict::Dict{String, Any})
    errors = String[]
    # TODO: keep the validation gap explicit until the operating-mode rule matrix is finalized.
    return errors
end
